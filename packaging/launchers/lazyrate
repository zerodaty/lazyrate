#!/usr/bin/python3
"""Launcher for lazyrate (CLI/TUI) using the vendored private prefix."""
import sys

sys.path.insert(0, "/usr/lib/lazyrate")

from lazyrate.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
